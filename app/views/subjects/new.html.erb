<%= link_to("<< Back to List", subjects_path, :class => 'back-link') %>

<div class="subjects new">
  <h2>Create Subject</h2>

  <%= form_for(@new_subject, :url => subjects_path, :method => 'post') do |f| %>
    <%# :url and :method can be left out and Rails would still know what to do with the newly
    # created object. In this example, we are just explicitly telling it what to do. %>
       
       
       <%= render(:partial =>'form', :locals => {:f => f})%>


    <div class="form-buttons">
      <%= f.submit("Create Subject") %>
    </div>

  <% end %>
</div>
