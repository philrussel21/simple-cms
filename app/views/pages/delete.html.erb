<%= link_to("<< Back to List", subject_path(params[:id]), :class => 'back-link') %>

<div class="delete page">
  <h2>Delete Page</h2>

  <%= form_for(@delete_page, :url => subject_page_path, :method => 'delete') do |f| %>
    <%# <%= form_for(@delete_page, :method => 'delete') do |f| %>
    <table>
    <tr>
    <th>Page Name: </th>
    <td><%=@delete_page.name%></td>
    </tr>
    </table>
    <p>Are you sure you want to delete this page?</p>


    <div class="form-buttons">
      <%= f.submit("Delete Page") %>
    </div>

  <% end %>
</div>
